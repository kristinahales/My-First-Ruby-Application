<div class='col-md-4'>
  <%# data-id is grabbing the specific portfolio item id for javascript file %>
  <div class="card" data-id="<%= portfolio_item.id%>">
    <%# portfolio_img tag comes from the method in app/helpers/portfolio_helper  %>
    <%= image_tag portfolio_img(portfolio_item.thumb_image, 'thumb'), width: '100%' %>
    <p class="card-text">
      <span><%= link_to portfolio_item.title, portfolio_show_path(portfolio_item.id) %></span> <%= portfolio_item.subtitle %>
    </p>
  </div>
</div>



