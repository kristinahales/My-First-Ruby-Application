<% if false %>
show the image unless it is null.

Different way of linking: 
we can create a link on the main portfolio items page that links to creating a new item
<a href="portfolios/new">Create New Item</a>
we can also create a link to edit each portfolio item:
a href="portfolio/<%portfolio_item.id %>/edit>Edit</a>

<% end %>




<h1>Portfolio Items</h1>

<%= link_to "Create New Item", new_portfolio_path %>

<% @portfolio_items.each do |portfolio_item| %>
    <p><%= link_to portfolio_item.title, portfolio_show_path(portfolio_item.id) %></p>
    <p><%= portfolio_item.substring %></p>
    <p><%= portfolio_item.body %></p>
    <%= image_tag portfolio_item.thumb_image unless portfolio_item.thumb_image.nil? %>
    <%= link_to "Edit", edit_portfolio_path(portfolio_item.id) %>
    <%= link_to 'Delete Portfolio Item', portfolio_show_path(portfolio_item), method: :delete, data: { confirm: 'Are you sure?' } %>
<% end %>
