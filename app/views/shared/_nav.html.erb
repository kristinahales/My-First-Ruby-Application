<%# Partials are always created in the views file and contain a _ before the name. %>
<%# We are able to access these nav bar components in more than one layout now. %>
<div class="<%= location %>">
    <div><%= link_to "Home", root_path %></div>
    <div><%= link_to "About", about_path %></div>
    <div><%= link_to "Contact", contact_path %></div>
    <div><%= link_to "Blog", blogs_path %></div>
    <div><%= link_to "Portfolio", portfolios_path %></div>
    <%# Checks for current user from app/helper/application_helper %>
    <%= login_helper 'nav-link' %>
</div>